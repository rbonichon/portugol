algoritmo "Area retangulo"
var altura, base: real
inicio
  escreval("Entre com valores positivo para a base do retângulo: ")
  leia(base, altura)
  escreval("A área do retângulo é:", altura * base)
fimalgoritmo
